# Project Rules

## Client Slider Rules
- **Reviews Auto-Play**: Any customer reviews slider/carousel must auto-slide with a 5-second interval.
- **Hover/Touch Pause**: The reviews slider must pause sliding immediately when hovered over (`onMouseEnter`) or touched on mobile (`onTouchStart`), and resume when the interaction ends (`onMouseLeave`).
